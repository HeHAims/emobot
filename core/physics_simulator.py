# core/physics_simulator.py

class PhysicsSimulator:
    def simulate(self, equations, initial_conditions):
        timeline = []  # build your timeline here
        # ...simulation logic...
        return timeline  # not a string
    
    def explain_formula(self, formula):
        return (
            formula
            .replace("+", " plus ")
            .replace("=", " equals ")
        )
